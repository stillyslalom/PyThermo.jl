[deps]
# Python itself still comes from the conda backend (pixi); the thermo stack
# is installed from PyPI via uv (see pip_backend preference in Project.toml).
python = ">=3.10,<4"

[pip.deps]
thermo = "~=0.6.0"
fluids = "~=1.3.0"
chemicals = "~=1.5.1"
